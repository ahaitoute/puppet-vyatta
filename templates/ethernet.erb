    ethernet <%= ethernet %> {
<% Array(address).each do |item| %>        address <%= item %>
<% end -%>
        duplex <%= duplex %>
        hw-id <%= hw_id %>
        smp_affinity <%= smp_affinity %>
        speed <%= speed %>
    }
